import { DefaultTheme } from "styled-components";
import { SpotifyTheme } from "../types/theme";

const spotifyTheme: SpotifyTheme = {
  backgroundColor: "#181818",
  colors: {
    primary: "cyan",
    secondary: "magenta",
  },
};

export { spotifyTheme };
